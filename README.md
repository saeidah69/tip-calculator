# Tip Calculator

A simple, no-dependency tip calculator that runs in the browser.

## Usage

Open `index.html` in any browser — no install or build step required.

## Features

- Enter a bill amount and tip percentage
- Splits the total across any number of people
- Resets with one click

## Licence

MIT
